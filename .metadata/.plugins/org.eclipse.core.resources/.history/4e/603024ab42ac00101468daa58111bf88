<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<%@ taglib uri="jakarta.tags.core" prefix="c" %>
<%@ taglib uri="jakarta.tags.fmt" prefix="fmt" %>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>${news.title} - ABC News</title>
    <link rel="stylesheet" href="${pageContext.request.contextPath}/assets/css/style.css">
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="container">
            <div class="header-top">
                <div class="logo">
                    <a href="${pageContext.request.contextPath}/">
                        <h1>ABC News</h1>
                    </a>
                </div>
                <div class="user-menu">
                    <c:choose>
                        <c:when test="${not empty sessionScope.user}">
                            <span>Xin chào, ${sessionScope.user.fullname}</span>
                            <c:if test="${sessionScope.user.role == 1}">
                                <a href="${pageContext.request.contextPath}/admin" class="btn-admin">Quản trị</a>
                            </c:if>
                            <c:if test="${sessionScope.user.role == 0}">
                                <a href="${pageContext.request.contextPath}/reporter" class="btn-reporter">Phóng viên</a>
                            </c:if>
                            <a href="${pageContext.request.contextPath}/logout" class="btn-logout">Đăng xuất</a>
                        </c:when>
                        <c:otherwise>
                            <a href="${pageContext.request.contextPath}/login" class="btn-login">Đăng nhập</a>
                        </c:otherwise>
                    </c:choose>
                </div>
            </div>
            
            <nav class="main-nav">
                <ul class="nav-menu">
                    <li><a href="${pageContext.request.contextPath}/">Trang chủ</a></li>
                    <c:forEach var="category" items="${categories}">
                        <li><a href="${pageContext.request.contextPath}/category?id=${category.id}">${category.name}</a></li>
                    </c:forEach>
                </ul>
            </nav>
        </div>
    </header>
    
    <!-- Main Content -->
    <main class="main-content">
        <div class="container">
            <div class="content-wrapper">
                <!-- Left Content -->
                <div class="left-content">
                    <!-- Breadcrumb -->
                    <nav class="breadcrumb">
                        <a href="${pageContext.request.contextPath}/">Trang chủ</a> > 
                        <a href="${pageContext.request.contextPath}/category?id=${news.categoryId}">${news.categoryName}</a> > 
                        <span>${news.title}</span>
                    </nav>
                    
                    <!-- News Detail -->
                    <article class="news-detail">
                        <header class="news-header">
                            <h1>${news.title}</h1>
                            <div class="news-meta">
                                <span class="category">${news.categoryName}</span>
                                <span class="author">Tác giả: ${news.authorName}</span>
                                <span class="date"><fmt:formatDate value="${news.postedDate}" pattern="dd/MM/yyyy HH:mm"/></span>
                                <span class="views">${news.viewCount} lượt xem</span>
                            </div>
                        </header>
                        
                        <div class="news-image">
                            <img src="${pageContext.request.contextPath}/assets/Images/${news.image}" alt="${news.title}">
                        </div>
                        
                        <div class="news-content">
                            ${news.content}
                        </div>
                    </article>
                    
                    <!-- Related News -->
                    <section class="related-news">
                        <h3>Tin cùng loại</h3>
                        <div class="related-news-list">
                            <c:forEach var="relatedNews" items="${relatedNewsList}">
                                <div class="related-news-item">
                                    <a href="${pageContext.request.contextPath}/news?id=${relatedNews.id}">
                                        <img src="${pageContext.request.contextPath}/assets/Images/${relatedNews.image}" alt="${relatedNews.title}">
                                        <h4>${relatedNews.title}</h4>
                                    </a>
                                </div>
                            </c:forEach>
                        </div>
                    </section>
                </div>
                
                <!-- Right Sidebar -->
                <div class="right-sidebar">
                    <!-- Most Viewed -->
                    <section class="most-viewed">
                        <h3>Tin được xem nhiều</h3>
                        <ul class="sidebar-news-list">
                            <c:forEach var="popularNews" items="${mostViewedNews}">
                                <li>
                                    <a href="${pageContext.request.contextPath}/news?id=${popularNews.id}">
                                        ${popularNews.title}
                                    </a>
                                    <span class="views">${popularNews.viewCount} lượt xem</span>
                                </li>
                            </c:forEach>
                        </ul>
                    </section>
                    
                    <!-- Newsletter Subscription -->
                    <section class="newsletter-signup">
                        <h3>Đăng ký nhận tin</h3>
                        <form action="${pageContext.request.contextPath}/newsletter" method="post">
                            <input type="email" name="email" placeholder="Nhập email của bạn" required>
                            <button type="submit">Đăng ký</button>
                        </form>
                    </section>
                </div>
            </div>
        </div>
    </main>
    
    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 ABC News. Tất cả quyền được bảo lưu.</p>
        </div>
    </footer>
</body>
</html>