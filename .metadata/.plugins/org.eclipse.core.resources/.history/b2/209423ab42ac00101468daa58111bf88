<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<%@ taglib uri="jakarta.tags.core" prefix="c" %>
<%@ taglib uri="jakarta.tags.fmt" prefix="fmt" %>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản lý tin của tôi - ABC News</title>
    <link rel="stylesheet" href="${pageContext.request.contextPath}/assets/css/style.css">
    <link rel="stylesheet" href="${pageContext.request.contextPath}/assets/css/admin.css">
</head>
<body>
    <!-- Header -->
    <header class="admin-header">
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <h1>ABC News - Phóng viên</h1>
                </div>
                <div class="user-info">
                    <span>Xin chào, ${sessionScope.user.fullname}</span>
                    <a href="${pageContext.request.contextPath}/" class="btn-home">Trang chủ</a>
                    <a href="${pageContext.request.contextPath}/logout" class="btn-logout">Đăng xuất</a>
                </div>
            </div>
        </div>
    </header>
    
    <!-- Reporter Navigation -->
    <nav class="admin-nav">
        <div class="container">
            <ul class="admin-menu">
                <li><a href="${pageContext.request.contextPath}/reporter">Trang chủ</a></li>
                <li><a href="${pageContext.request.contextPath}/reporter/news" class="active">Quản lý tin của tôi</a></li>
                <li><a href="${pageContext.request.contextPath}/reporter/news/add">Viết tin mới</a></li>
            </ul>
        </div>
    </nav>
    
    <!-- Main Content -->
    <main class="admin-content">
        <div class="container">
            <div class="page-header">
                <h2>Quản lý tin tức của tôi</h2>
                <a href="${pageContext.request.contextPath}/reporter/news/add" class="btn-primary">Viết tin mới</a>
            </div>
            
            <!-- Search and Filter -->
            <div class="search-filter">
                <form action="${pageContext.request.contextPath}/reporter/news" method="get" class="search-form">
                    <input type="text" name="search" placeholder="Tìm kiếm tin tức..." value="${param.search}">
                    <select name="category">
                        <option value="">Tất cả loại tin</option>
                        <c:forEach var="category" items="${categories}">
                            <option value="${category.id}" <c:if test="${param.category == category.id}">selected</c:if>>
                                ${category.name}
                            </option>
                        </c:forEach>
                    </select>
                    <select name="sortBy">
                        <option value="date" <c:if test="${param.sortBy == 'date'}">selected</c:if>>Ngày đăng</option>
                        <option value="views" <c:if test="${param.sortBy == 'views'}">selected</c:if>>Lượt xem</option>
                        <option value="title" <c:if test="${param.sortBy == 'title'}">selected</c:if>>Tiêu đề</option>
                    </select>
                    <button type="submit" class="btn-search">Tìm kiếm</button>
                </form>
            </div>
            
            <!-- News Table -->
            <div class="data-table">
                <table>
                    <thead>
                        <tr>
                            <th>Tiêu đề</th>
                            <th>Loại tin</th>
                            <th>Ngày đăng</th>
                            <th>Lượt xem</th>
                            <th>Trang chủ</th>
                            <th>Thao tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        <c:forEach var="news" items="${myNewsList}">
                            <tr>
                                <td class="news-title">
                                    <a href="${pageContext.request.contextPath}/news?id=${news.id}" target="_blank">
                                        ${news.title}
                                    </a>
                                </td>
                                <td>${news.categoryName}</td>
                                <td><fmt:formatDate value="${news.postedDate}" pattern="dd/MM/yyyy"/></td>
                                <td>${news.viewCount}</td>
                                <td>
                                    <c:choose>
                                        <c:when test="${news.home == 1}">
                                            <span class="badge badge-success">Có</span>
                                        </c:when>
                                        <c:otherwise>
                                            <span class="badge badge-secondary">Không</span>
                                        </c:otherwise>
                                    </c:choose>
                                </td>
                                <td class="actions">
                                    <a href="${pageContext.request.contextPath}/news?id=${news.id}" 
                                       class="btn-view" target="_blank" title="Xem">👁️</a>
                                    <a href="${pageContext.request.contextPath}/reporter/news/edit?id=${news.id}" 
                                       class="btn-edit" title="Sửa">✏️</a>
                                    <a href="${pageContext.request.contextPath}/reporter/news/delete?id=${news.id}" 
                                       class="btn-delete" title="Xóa" 
                                       onclick="return confirm('Bạn có chắc muốn xóa tin tức này?')">🗑️</a>
                                </td>
                            </tr>
                        </c:forEach>
                    </tbody>
                </table>
            </div>
            
            <c:if test="${empty myNewsList}">
                <div class="empty-state">
                    <h3>Bạn chưa có tin tức nào</h3>
                    <p>Hãy bắt đầu viết tin tức đầu tiên của bạn!</p>
                    <a href="${pageContext.request.contextPath}/reporter/news/add" class="btn-primary">Viết tin mới</a>
                </div>
            </c:if>
            
            <!-- Pagination -->
            <c:if test="${totalPages > 1}">
                <div class="pagination">
                    <c:if test="${currentPage > 1}">
                        <a href="?page=${currentPage - 1}&search=${param.search}&category=${param.category}&sortBy=${param.sortBy}" 
                           class="prev">« Trước</a>
                    </c:if>
                    
                    <c:forEach begin="1" end="${totalPages}" var="i">
                        <c:choose>
                            <c:when test="${i == currentPage}">
                                <span class="current">${i}</span>
                            </c:when>
                            <c:otherwise>
                                <a href="?page=${i}&search=${param.search}&category=${param.category}&sortBy=${param.sortBy}">${i}</a>
                            </c:otherwise>
                        </c:choose>
                    </c:forEach>
                    
                    <c:if test="${currentPage < totalPages}">
                        <a href="?page=${currentPage + 1}&search=${param.search}&category=${param.category}&sortBy=${param.sortBy}" 
                           class="next">Sau »</a>
                    </c:if>
                </div>
            </c:if>
            
            <c:if test="${not empty message}">
                <div class="message ${messageType}">
                    ${message}
                </div>
            </c:if>
        </div>
    </main>
    
    <!-- Footer -->
    <footer class="admin-footer">
        <div class="container">
            <p>&copy; 2024 ABC News Reporter Panel. Tất cả quyền được bảo lưu.</p>
        </div>
    </footer>
</body>
</html>